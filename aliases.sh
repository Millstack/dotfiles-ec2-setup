#!/bin/bash

# Navigation
alias ..="cd .."
alias ...="cd ../.."

# Listing
alias ll="ls -lahF --color=auto"
alias l="ls -CF"
alias lst="ls -ltr"

# Git Shortcuts
alias gst="git status"
alias ga="git add"
alias gc="git commit -m"
alias gp="git push"

# Cloud/DevOps
alias myip="curl ifconfig.me"
alias tf="terraform"
alias k="kubectl"
